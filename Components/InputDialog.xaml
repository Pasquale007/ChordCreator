<Window x:Class="ChordCreater.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input Dialog" Height="150" Width="300">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Name="Heading" Text="Enter Chord" FontWeight="Bold"/>
            <TextBlock Name="Text" Text="Enter the chord for the selected word:" Margin="0,5,0,0"/>
            <TextBox x:Name="ChordTextBox" Margin="0,5,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="OK" Click="OkButton_Click" Margin="0,0,5,0"/>
                <Button Content="Cancel" Click="CancelButton_Click"/>
            </StackPanel>
            <StackPanel Name="Lines"/>
        </StackPanel>
    </Grid>
</Window>
